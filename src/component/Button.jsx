const Button = ({ style }) => (
  <button
    className={`px-6 py-3 bg-blue-gradient text-xl font-semibold active:outline-none text-primary ${style} rounded-[10px]`}
  >
    Get Started
  </button>
);

export default Button;
